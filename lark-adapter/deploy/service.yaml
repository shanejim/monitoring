apiVersion: v1
kind: Service
metadata:
  name: lark-alertmanager-adapter
  labels:
    app.kubernetes.io/name: lark-alertmanager-adapter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lark-alertmanager-adapter
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
